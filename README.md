# builtin

`builtin` 提供框架默认注册项（基础状态、类型、默认行为）。

## 安装

```bash
go get github.com/infrago/builtin@latest
```

## 使用

```go
import _ "github.com/infrago/builtin"
```

通常配合 `infra.Run()` 即可。

## 公开 API（摘自源码）

- 见源码导出函数
